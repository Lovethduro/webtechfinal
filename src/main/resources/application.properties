# Application Name
spring.application.name=tech

# MongoDB Configuration
spring.data.mongodb.uri=mongodb+srv://clients:Rufus1122@cluster0.sfffjuu.mongodb.net/user_images?retryWrites=true&w=majority&appName=Cluster0&maxPoolSize=10&minPoolSize=1&maxIdleTimeMS=30000&connectTimeoutMS=10000&socketTimeoutMS=15000&serverSelectionTimeoutMS=10000
spring.data.mongodb.database=user_images

# Disable JPA auto-configuration (since you're using MongoDB)
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration,org.springframework.boot.autoconfigure.orm.jpa.HibernateJpaAutoConfiguration

# Thymeleaf Configuration
spring.thymeleaf.cache=false
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.check-template-location=true

# Security Configuration
spring.security.filter.order=100

# JWT Configuration
jwt.secret=TmV4YVN1cGVyU2VjcmV0SldUS2V5XzIwMjVAU3Ryb25nQXV0aCEKCg==
jwt.expiration=86400000

# Session Configuration - FIXED for MongoDB serialization issues
spring.session.store-type=none
# Commented out MongoDB session storage as it can cause serialization issues
# spring.session.mongodb.collection-name=sessions
spring.session.timeout=30m
server.servlet.session.tracking-modes=cookie
server.servlet.session.cookie.max-age=1800
server.servlet.session.cookie.http-only=true
server.servlet.session.cookie.secure=false

# File upload size limits
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB

# Server Configuration
server.port=${PORT:8080}
server.error.include-message=always
server.error.include-binding-errors=always
server.error.include-stacktrace=on_param

# Static Resources
spring.mvc.static-path-pattern=/**
spring.web.resources.static-locations=classpath:/static/,classpath:/public/

# Logging - Reduced for production
logging.level.org.springframework.web=INFO
logging.level.org.springframework.data.mongodb=INFO
logging.level.org.mongodb.driver=WARN
logging.level.com.web.tech=DEBUG
logging.level.org.springframework.transaction=INFO
logging.level.org.springframework.security=INFO

# Remove Hibernate logging since you're not using JPA
# logging.level.org.hibernate.SQL=DEBUG
# logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# DevTools
spring.devtools.restart.enabled=false

# Production Profile Settings
spring.profiles.active=${SPRING_PROFILES_ACTIVE:default}

# MongoDB Connection Pool Settings (additional safety)
spring.data.mongodb.option.connection-timeout=10000
spring.data.mongodb.option.socket-timeout=15000
spring.data.mongodb.option.max-connection-idle-time=30000
spring.data.mongodb.option.max-connection-life-time=60000